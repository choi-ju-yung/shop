<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CustomCenter">

	<!-- 게시글 리스트 페이징 -->
	<select id="getPagedBoardList" parameterType="Map"
		resultType="com.example.demo.board.vo.Board">

		SELECT * FROM (
		SELECT ROWNUM rnum, A.*
		FROM (
		SELECT *
		FROM BOARD
		WHERE NOTICE_YN = #{notice}
		<if test="category != null and category != ''">
			AND BOARD_CATEGORY = #{category}
		</if>
		ORDER BY BOARD_NO DESC
		) A
		WHERE ROWNUM <![CDATA[<= ]]>
		#{offset} + #{limit}
		)
		WHERE rnum <![CDATA[ > ]]>
		#{offset}
	</select>

	<select id="getTotalBoardCount" resultType="int">
		SELECT COUNT(*) FROM board WHERE notice_yn = #{notice}
		<if test="category != null and category != ''">
			AND BOARD_CATEGORY = # {category}
		</if>
	</select>
	
	<select id="getBoardList" resultType="com.example.demo.board.vo.Board">
		SELECT * FROM BOARD
	</select>
	
	<select id="getBoardListByCategory" resultType="com.example.demo.board.vo.Board">
		SELECT * FROM BOARD WHERE BOARD_CATEGORY = #{category}
	</select>
</mapper>